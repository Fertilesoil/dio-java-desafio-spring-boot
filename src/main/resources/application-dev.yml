spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/db_books
    username: postgres
    password: febredorato
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.PostgreSQLDialect
      format_sql: true